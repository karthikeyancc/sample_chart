{
  "meta": {
    "title": "ECharts hierarchical UI schema",
    "version": "2025-11-27",
    "notes": "Use this to generate nested Angular reactive forms. Each node has 'type'; objects have 'props'; arrays have 'items'."
  },
  "general":{
    "Data-URL": {
      "type": "string",
      "caption":" Data URL ",
      "default": "https://microsoftedge.github.io/Demos/json-dummy-data/64KB.json",
      "table-default": "http://localhost:4200/rwapi?qu=test_data",
      "tree-default": "http://localhost:4200/assets/echarts-tree-data.json",
      "hint": "URL to fetch data from (JSON array of objects).",
      "event-name": "data-url-changed"
    },
    "Chart-Type": {
      "type": "select",
      "caption":" Chart Type ",
      "default": "line",
      "hint": "Type of chart to render.",
      "event-name": "chart-type-changed"
    },
    "Major-Axis": {
      "type": "select",
      "caption":" Major Axis ", 
      "enum": [],
      "hint": "Major axis column to be rendered.",
      "listener":"json-fetched"
    },
    "Direction": {
      "type": "select",
      "enum": ["Vertical","Horizontal"],
      "hint": "Horixontal or vertical layout (for bar charts)."
    },
    "Breaks": {
      "type": "boolean",
      "default": false,
      "hint": "Introduce axis breaks for large gaps in data."
    },
    "show-label": {
      "type": "boolean",
      "default": false,
      "caption":" Show Labels ",
      "key":"label.show",
      "hint": "Introduce axis breaks for large gaps in data."
    },
    "label-rotate": {
      "caption":" Label Rotation ",
      "key":"label.rotate",
      "type": "number",
      "minimum": -90,
      "maximum": 90,
      "default": 0,
      "hint": "Introduce axis breaks for large gaps in data."
    },
    "label-align-horizontal": {
      "type": "select",
      "caption":" Label Horizontal Alignment ",
      "key":"label.align",
      "enum": ["left","center","right"],
      "default": "center",
      "hint": "Introduce axis breaks for large gaps in data."
    },
    "label-align-vertical": {
      "type": "select",
      "caption":" Label Vertical Alignment ",
      "key":"label.verticalAlign",
      "enum": ["top","middle","bottom"],
      "default": "middle",
      "hint": "Introduce axis breaks for large gaps in data."
    },
    "label-position": {
      "type": "select",
      "caption":" Label Position ",
      "key":"label.position",
      "enum": ["left","right","top","bottom","inside","insideLeft","insideRight","insideTop","insideBottom","insideTopLeft","insideBottomLeft","insideTopRight","insideBottomRight"],
      "default": "middle",
      "hint": "Introduce axis breaks for large gaps in data."
    },
    "label-distance": {
      "type": "number",
      "minimum": 0,
      "default": 10,
      "hint": "Distance of label from axis."
    }
  }
}
